package file;

import org.apache.log4j.Logger;

public class StudentScheduleFileWriter {
	
	private static final Logger LOGGER = Logger.getLogger(StudentScheduleFileWriter.class);

	public String generateContent() {
		StringBuilder sb = new StringBuilder();
		
		return sb.toString();
	}
}
